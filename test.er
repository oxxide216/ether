(fun main ()
  (let c (counter))
  (println f"{(c)}")
  (println f"{(c)}")
  (println f"{(c)}"))

(fun counter ()
  (let count 0)
  (fun ()
    (let prev count)
    (set count (+ count 1))
    prev))
