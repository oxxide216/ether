(fun main ()
  (let c (counter))
  (println i"{(c)}")
  (println i"{(c)}")
  (println i"{(c)}"))

(fun counter ()
  (let count 0)
  (fun ()
    (let prev count)
    (set count (+ count 1))
    prev))
